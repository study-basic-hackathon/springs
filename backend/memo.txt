api/

各エンドポイントごとにファイルを分けると見通しがよい

queue.ts は待ち時間投稿系、stores.ts は店舗情報系

config/

環境変数・設定をまとめる場所

他のファイルから import { ENV } from "../config/env" で参照

db/

MongoDB 接続用ファイルだけまとめる

接続の再利用や切断処理もここで管理

utils/

TTLインデックスや古い投稿の自動削除などの共通処理

server.ts

Express のエントリーポイント

各 api をまとめてルーティング